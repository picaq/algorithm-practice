let insertBiTree = (val, tree) => {
    // create pointer variable and copy tree into it

    // create a while loop that checks for the truthiness of pointer in order to continue looping
        // if val > pointer value AND the right of the pointer is not null
            // move pointer to the right

        // otherwise if the left of the pointer is not null
            // move pointer to the left
        
        // otherwise
            // if left of pointer is null
                // set the left of pointer to val
            // else if right of pointer is null
                // set the right of pointer to val

    // return tree
}